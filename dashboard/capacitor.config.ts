import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prestamosedin.dashboard',
  appName: 'PrestamosEdin Dashboard',
  webDir: 'build'
};

export default config;
